dist: xenial
language: python
python:
- 3.6
- 3.7

addons:
  apt:
    packages:
    - slapd
    - ldap-utils

install:
  - pip install pipenv
  - pipenv install --dev

script:
  - pipenv run python -m pytest
